#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    // vector<int> sieve(2e5+1,-1);
    // vector<int> primes;
    // for(int i=2;i<=2e5;i++){
    //     if(sieve[i]==-1){
    //         primes.push_back(i);
    //         for(int j=1;i*j<=2e5;j++){
    //             sieve[i*j]=i;
    //         }
    //     }
    // }
    while(t--){
        int n;
        cin>>n;
        vector<int> vec(n);
        for(int i=0;i<n;i++)cin>>vec[i];
    }
}